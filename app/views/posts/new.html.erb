<div class="max-w-2xl min-w-[320px] mx-auto my-8">
  <article class="prose">
    <div class="sm:flex sm:justify-between sm:items-start gap-2">
      <h1 class="text-3xl font-bold mb-2">New post</h1>
      <div class="flex flex-col sm:flex-row sm:items-center space-y-2 sm:space-y-0 sm:space-x-4">
        <%= link_to "Cancel", posts_path, class: "text-blue-600 hover:text-blue-800" %>
      </div>
    </div>

    <%= render "form", post: @post %>
  </article>
</div>
